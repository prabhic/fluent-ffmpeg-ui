define(['./module'], function (controllers) {
    'use strict';

    controllers.controller('OutputCtrl', ['$scope', '$state', 'nwService', function ($scope, $state,nwService) {

        $scope.outputResult = "final output";
        
            
    }]);
});
